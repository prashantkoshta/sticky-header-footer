import { StickyContentDirective } from './sticky-content.directive';

describe('StickyContentDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyContentDirective();
    expect(directive).toBeTruthy();
  });
});
